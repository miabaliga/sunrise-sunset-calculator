version: '3.8'

services:
  web:
    build: .
    container_name: sunrise-sunset-calculator
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
